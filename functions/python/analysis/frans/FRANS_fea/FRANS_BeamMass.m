function [ Kbg, Mbg ]=FRANS_BeamMass( utils, Ks, Ms )

%Set constants for readibility
nsections = utils.beam.npatch_1d;
ncand = utils.opt.ncand;

for b=1:nsections %Loop each section in the beam
    
    
end

end